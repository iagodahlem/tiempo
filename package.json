{
  "name": "tiempo",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run --parallel start",
    "build": "lerna run --stream build",
    "test": "lerna run --stream test",
    "deploy": "lerna run --stram deploy"
  },
  "devDependencies": {
    "lerna": "^2.5.1"
  }
}
